import * as actionTypes from '../actionTypes';

const initState = {
	loading: false,
}

function loading(state = initState, action) {
	switch (action.type) {

		default:
			return state;
	}
}

export default loading;