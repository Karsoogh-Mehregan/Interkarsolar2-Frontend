import * as actionTypes from '../actionTypes';

const initState = {
    isFetching: false,
    redirectTo: null,
    force: false,
};

function exam(state = initState, action) {
    switch (action.type) {

        default:
            return state;
    }
}

export default exam;
